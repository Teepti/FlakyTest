version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: cimg/maven:3.6.3-jdk-8
    steps:
      - checkout
      - run: mvn test
  test:
    docker:
      - image: cimg/maven:3.6.3-jdk-8
    steps:
      - checkout
      - run: mvn test

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build
      - test